<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.unipampa.appavaliacoes">

    <application
        android:name=".Service.Channel"
        android:allowBackup="true"
        android:icon="@drawable/ic"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controller.MainActivity">




        </activity>

        <activity android:name=".Service.NotificationReceiver">


        </activity>
        <activity android:name=".Controller.TipoNotificacao">

        </activity>

        <activity android:name=".Controller.AdicionarAvaliacaoActivity">

        </activity>

        <activity android:name=".Controller.NotificationChannel">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>

        <receiver android:name=".Service.NotificationReceiver"></receiver>

    </application>

</manifest>